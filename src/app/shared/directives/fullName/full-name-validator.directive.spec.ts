import { FullNameValidatorDirective } from './full-name-validator.directive';

describe('FullNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FullNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
