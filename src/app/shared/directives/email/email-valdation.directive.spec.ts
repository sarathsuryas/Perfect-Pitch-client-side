import { EmailValidationDirective } from "./email-valdation.directive";

describe('EmailValdationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidationDirective();
    expect(directive).toBeTruthy();
  });
});
